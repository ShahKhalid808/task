/// all common components should be exported from here. so we can reduce the code lenght in our screens.

import Wrapper from './wrapper';
import TextInput from './textInput';
import * as Icons from './icon';
import Spacer from './spacer';
import Text from './text';

export {Wrapper, TextInput, Icons, Spacer, Text};
